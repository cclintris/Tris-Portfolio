export enum E_buttonType {
  primary = 'primary',
  success = 'success',
  warning = 'warning',
  error = 'error',
  default = 'default',
}

export enum E_buttonShape {
  circle = 'circle',
  round = 'round',
  default = 'default',
}
