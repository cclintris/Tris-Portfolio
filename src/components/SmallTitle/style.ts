import styled from 'styled-components';

export const StyledSmallTitle = styled.div`
  display: flex;
  align-items: center;

  p {
    padding-right: 1rem;
    svg {
      font-size: 2.5rem;
    }
  }

  h3 {
    color: var(--white-color);
    font-size: 1.5rem;
  }
`;
