import styled from 'styled-components';

export const StyledHobbySection = styled.div`
  .hobbies {
    margin-top: 2.5rem;
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-start;
    /* display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-row-gap: 2.5rem;
    grid-column-gap: 2rem; */
  }
`;
